# Optional audio-transcription dependencies. Installed via
# `ugcspy install-deps --with-audio` — opt-in because openai-whisper
# brings in torch (~700MB). decode.py degrades gracefully if these
# aren't installed — it just skips the audio stage and tells the user
# to re-run install-deps with --with-audio.
#
# Without these: /ugcspy-decode + /ugcspy-remix work but only see
# on-screen overlay text, missing the spoken narrative.
openai-whisper>=20231117
imageio-ffmpeg>=0.4.9  # static ffmpeg binary - transcript mode works with ZERO system deps (no brew/apt ffmpeg)
