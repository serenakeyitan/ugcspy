# recipes/

Run outputs land here, one folder per analyzed video: `recipes/<video-id>/`.

Contents are gitignored — see `.gitignore` at repo root. Don't commit downloaded videos or generated artifacts.

If you want to share a specific recipe for discussion, link it in an issue or attach the `recipe.json` directly to a comment.
